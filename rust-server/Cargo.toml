[package]
name = "astro_nomi_server"
version = "0.1.0"
edition = "2021"

[dependencies]
axum = "0.7"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "fs", "net"] }
tower-http = { version = "0.6", features = ["fs"] }
